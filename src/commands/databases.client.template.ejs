import type { Client } from 'appwrite';
<% for (var i = 0; i < ClassNames.length; i++) { %>
    import { <%- ClassNames[i] %>Service } from './schemas/client.js';
<% } %>

export class Databases {
    protected client: Client;

    constructor(client: Client) {
        this.client = client;
    }

    <% for (var i = 0; i < ClassNames.length; i++) { %>
        public get <%- ClassNames[i] %>() {
            return new <%- ClassNames[i] %>Service(this.client);
        }
    <% } %>
}

export { Client, Query, AppwriteException, Account, Avatars, Functions, Graphql, Locale, Storage, Teams, Models, Payload, RealtimeResponseEvent, UploadProgress, QueryTypes, QueryTypesList, Permission, Role, ID } from 'appwrite';